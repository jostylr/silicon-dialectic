<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }} — {{ page.edition_label | xml_escape }}</title>
  <subtitle>New edition created September 2026; entry dates identify the July 2025 prompt sequence.</subtitle>
  <link href="{{ page.url | absolute_url | xml_escape }}" rel="self" type="application/atom+xml"/>
  <link href="{{ page.edition_url | absolute_url | xml_escape }}" rel="alternate" type="text/html"/>
  <id>{{ page.edition_url | absolute_url | xml_escape }}</id>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author><name>{{ site.author.name | xml_escape }}</name><uri>{{ site.author.url | xml_escape }}</uri></author>
  {% assign feed_entries = site[page.collection_key] | sort: 'date' | reverse %}
  {% for post in feed_entries %}
  <entry>
    <title>{{ post.title | xml_escape }} — {{ page.edition_label | xml_escape }}</title>
    <link href="{{ post.url | absolute_url | xml_escape }}" rel="alternate" type="text/html"/>
    <id>{{ post.url | absolute_url | xml_escape }}</id>
    <published>{{ post.modify_date | date_to_xmlschema }}</published>
    <updated>{{ post.modify_date | date_to_xmlschema }}</updated>
    <summary>{{ post.description | xml_escape }} (Original prompt: {{ post.date | date: '%B %-d, %Y' }}.)</summary>
  </entry>
  {% endfor %}
</feed>
